# 依赖（体积大，可由 package-lock 还原）
node_modules/

# 构建/渲染产物
remotion-app/out/
out/
dist/

# 系统/编辑器
.DS_Store
*.log

# 密钥与本地配置（防泄露）
config.env
*.key
.env
.env.local

# 素材文件（体积大，不进 git；可由小云雀/Pexels 重新生成获取）
# Remotion public 整目录为素材
remotion-app/public/
# projects 下的素材二进制（保留 .md 文档与 .txt 清单）
projects/**/*.png
projects/**/*.jpg
projects/**/*.jpeg
projects/**/*.mp3
projects/**/*.mp4
projects/**/*.wav
projects/**/*.m4a

# 小云雀/配音临时调试日志（含 thread_id 等临时数据，无保留价值）
projects/**/poll/
projects/**/submit_logs/
projects/**/submit_logs2/
projects/**/log*.txt

# 项目根目录散落的原始素材/口播大文件 + 拉片临时产物（不进 git）
/*.mov
/*.mp4
/*.png
/*.wav
/*.jpg
_*_frames/
_lapian_yt/
保时捷轧空_交付包_*/
保时捷轧空_交付包_*.zip
